services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alfajr-elearning
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
